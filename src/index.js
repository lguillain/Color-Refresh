import React from 'react';
import ReactDOM from 'react-dom/client';
import TaskbaseColorPalettes from './color-palette-options';
import './index.css';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <TaskbaseColorPalettes />
  </React.StrictMode>
); 